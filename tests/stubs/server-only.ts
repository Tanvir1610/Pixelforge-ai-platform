// Test stub for Next.js's `server-only` guard, which has no runtime export.
// The real guard still fails the build if a server module is imported into a
// client bundle; this only satisfies the module resolver under Vitest.
export {};
